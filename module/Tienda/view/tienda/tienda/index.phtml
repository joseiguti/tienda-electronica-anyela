<?php

$title = 'My Products';
$this->headTitle($title);
?>
<h1><?= $this->escapeHtml($title) ?></h1>
<p>
    <a href="<?= $this->url('tienda', ['action' => 'record']) ?>">Add new product</a>
</p>

<table class="table">
    <tr>
        <th>Nombre</th>
        <th>Precio</th>
        <th>Cantidad</th>
        <th>&nbsp;</th>
    </tr>
    <?php foreach ($productos as $producto) : ?>
        <tr>
            <td><?= $this->escapeHtml($producto->nombre) ?></td>
            <td><?= $this->escapeHtml($producto->precio) ?></td>
            <td><?= $this->escapeHtml($producto->cantidad) ?></td>
            <td>
                <a href="<?= $this->url('tienda', ['action' => 'record', 'id' => $producto->id]) ?>">Edit</a>
                <a href="<?= $this->url('tienda', ['action' => 'delete', 'id' => $producto->id]) ?>">Delete</a>
            </td>
        </tr>
    <?php endforeach; ?>
</table>